# ModPlus

ModPlus is a simple hashing algorithm. It takes in a positive integer X and returns a combinatorial sum of the binary representation of X after it is split into chunks of given length.

For example, `modplus(12, chunk_length=3)` should return 6.
